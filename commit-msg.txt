The primer goes behind a door, and the chronicle folds shut

OP-23, second of three. The premise and the paragraph on how a year is spent are
now behind "what this is". The page keeps one line of them - the epigraph, as the
header's subtitle in place of "prototype", because it is one line and
concept/concept.md says every strategic decision in the game is a position on it.

This reverses 7623cee on purpose and the register says so. That commit put the
blurb on the page specifically so a first-time player learned the ratchet before
being asked to decide anything about it, and nothing else on the page teaches it -
the key under the board names the colours and does not say that one erases the
other. If somebody who has never seen this opens it and the ratchet surprises
them at year five, the fix is a line in the key, not the paragraphs coming back.

"what happened" folds shut. It was already a <details>, but render() forced it
open on every frame, so closing it lasted until the next click and the panel
could not actually be put away. The label is still written from render(); the
open state is now the player's.

No rules changed. smoke.js passes 11,758 checks.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01BmcC97TZ38RejXMSsFmLp9
