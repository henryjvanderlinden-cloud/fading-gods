The terms panel was offering a checkbox called "undefined"

1.24 put `roam` in `FG.R2BUILT` and never gave it a name. The rule list stopped
being a second copy when the engine started exporting R2BUILT — A-16 — but the
rule *names* are still a hand-kept map in `ui.js`, so the terms panel has been
showing a live, working switch labelled **undefined** since `2f25ef3`, four
commits ago. Rick found it by opening the panel.

It now reads *herds go where they will, and no hand reaches them*.

A missing label cannot be caught by counting anything: the row is the right
length, the checkbox works, the rule toggles correctly. Nothing was broken except
the one thing a player actually reads. So the check goes in against the engine's
own list, in both directions — every flag in the panel has a name, and the panel
offers every flag the engine has. That is A-16's lesson for the third time, and
the third distinct way this pair of lists has drifted.

Checks: 11,978, all passing.
